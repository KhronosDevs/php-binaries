#!/usr/bin/env bash

DIR="$(cd "$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")" && pwd)"

exec "$DIR/php-bin" \
  -n \
  -c "$DIR/php.ini" \
  -d "extension_dir=$DIR/../lib/modules" \
  -d "zend_extension=$DIR/../lib/php/extensions/no-debug-zts-20220829/opcache.so" \
  "$@"
